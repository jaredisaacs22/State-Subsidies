# ─── App ────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ─── Database ────────────────────────────────────────────────────────────────
# SQLite (local dev) — Prisma will create subsidies.db automatically
DATABASE_URL="file:./subsidies.db"

# PostgreSQL (production) — uncomment and fill in:
# DATABASE_URL="postgresql://user:password@localhost:5432/subsidies?schema=public"

# ─── Scraper (optional) ──────────────────────────────────────────────────────
# Set to "false" to run live scrapers instead of mock data
SCRAPER_MOCK_MODE=true
